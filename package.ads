package Package_Name is

-- public declarations

private

-- private declarations

end
Package_Name;

--An entity declared NOT in the private part of a package in Ada is roughly equivalent to a public member of a C++ or Java class. 
--An entity declared in the private part of a package in Ada is roughly equivalent to a protected member of a C++ or Java class. 
--An entity declared in the body of an Ada package is roughly equivalent to a private member of a C++ or Java class.